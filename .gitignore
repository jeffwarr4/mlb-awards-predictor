# Python
__pycache__/
*.pyc
.env
venv/

# Local overrides — credentials, personal paths (never commit)
config_local.py
FG App Password.txt

# Local data inputs — ignore everything except FG manual exports
data/*
!data/raw/
data/raw/*
!data/raw/fg_exports/

# Model binaries — large files, not tracked
models/

# Prediction archives and prev-run snapshots (keep _latest.csv and flat CSVs)
predictions/**/*_2[0-9][0-9][0-9][0-9]_*.csv
predictions/**/.prev_*.csv
